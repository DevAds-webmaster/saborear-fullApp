# Manager

Mini app de administración de usuarios (alta + listado) para uso local.

## Requisitos

- `VITE_BACKEND_URL`
- `VITE_BACKEND_KEY`

Configurar en `.env`.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run lint`
